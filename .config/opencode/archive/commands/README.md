# Archived OpenCode Commands

Retired global OpenCode slash commands. Claude Code commands under
`~/.claude/commands/` are canonical.

These files stay here only as migration reference and should not be copied back
to `~/.config/opencode/commands/` unless OpenCode-specific behavior is
explicitly needed again.
