#!/usr/bin/env bash
# Mock pi: emits a FAIL verdict with one critical blocker.
if [ "$1" = "--version" ]; then
  echo "0.99.0"
  exit 0
fi
cat >/dev/null 2>&1 || true
cat <<'EOF'
Found a problem.

```json
{
  "phase": "VERIFY",
  "verdict": "FAIL",
  "blockers": [
    {
      "id": "x",
      "severity": "critical",
      "summary": "Off-by-one in loop body",
      "detail_md": "Loop iterates one too many times.",
      "evidence": ["src/foo.ts:42"]
    }
  ],
  "suggestions": [],
  "summary_md": "Mock pi FAIL verdict.",
  "raw_model_id": "mock/fail",
  "schema_version": 1,
  "generated_at": "2026-05-10T00:00:00.000Z"
}
```
EOF
