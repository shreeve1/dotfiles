---
topic: tralph overhaul: kanban tickets -> dependency graph -> parallel worktrees -> merge
goal: evidence-based redesign of the tralph loop for parallel unattended execution
mode: autonomous
updated: 2026-09-10T16:44
---

- (insight by coach) EVIDENCE: ralph-df loop died mid-iteration-3 at 16:33 (log alive, no tmux/processes) - crash-only recovery is a hard requirement
- (insight by coach) EVIDENCE: ralph-loop run stopped entirely on first BLOCKED #269 - one blocked ticket kills whole run
- (insight by coach) EVIDENCE: current dotfiles board width: 5 tickets, ready frontier width 2 - parallelism exists but is modest
- (insight by coach) EVIDENCE: gralph already implements dependency-frontier waves with --jobs N for GitHub issues; dsh-board already does worktree lanes + ff-only merge per card; AgentTeams has DAG tasks but no worktree isolation
- (technique by coach) started First Principles Thinking
- (idea by coach) board IS the graph: no new graph format, frontier = query over blocked_by frontmatter (gralph already does this for gh issues)
- (idea by coach) scheduler as tiny pure function ready(issues)->set; everything else adapters
- (idea by coach) one worktree per ticket: lane branch auto/<id>, created lazily at claim time
- (idea by coach) merge in topological order: blockers land before dependents
- (idea by coach) chain lanes: dependent branches FROM blocker lane tip (max parallelism, fragile)
- (idea by coach) simpler: dependents branch from integration branch only after blockers LANDED - serializes chains, parallelizes independent subtrees
- (idea by coach) merge as a synthetic ticket: integrate node that every leaf blocks - reuses the same loop for the merge step
- (idea by coach) conflict prediction at graph time: intersect declared file scopes; overlapping tickets get a synthetic dependency edge (serialize instead of parallel)
- (idea by coach) no declared scopes? cheap parallel scout pass predicts touched files per ticket before scheduling
- (idea by coach) crash-only: all state in ticket frontmatter (status, lane, owner, heartbeat) + git worktree list; restarted driver reconstructs everything
- (idea by coach) driver stays dumb bash; intelligence stays in worker skills (Ralph philosophy)
- (idea by coach) inversion: kill bash driver, AgentTeams captain IS the scheduler; members get 'work only in <worktree>' execution prompts
- (idea by coach) verify twice: in lane before merge, and re-verify integration branch after each landing (catches semantic conflicts git merges cleanly)
- (idea by coach) semantic conflict (both lanes green, merged red) auto-opens a repair ticket into the same graph
- (idea by coach) progress.md will conflict across lanes: per-lane progress.d/<id>.md, driver concatenates on landing
- (idea by coach) board writes ONLY by driver on main; workers never touch .kanban in their lane - kills a whole conflict class
- (technique by coach) started SCAMPER
- (idea by coach) substitute tmux monitoring with systemd-run transient units per lane (already used for memory caps) - free restarts, journal, OOM handling
- (idea by coach) combine: port gralph's wave/frontier engine to read .kanban instead of gh - 90% of parallel logic already written and tested
- (idea by coach) adapt dsh-board lane mechanics wholesale: laneBranches, ff-only landing, bounce-with-reason
- (idea by coach) extend sentinel protocol: RALPH_RESULT MERGED/CONFLICT variants
- (idea by coach) review tier by diff: docs-only tickets skip the fresh-session review
- (idea by coach) reverse the merge: continuous merge queue (land+rebase one lane at a time) instead of big-bang merge at end - conflicts surface early and small
- (idea by coach) merge step IS the review: integrator session reviews the integrated diff as it lands each lane
- (idea by coach) adaptive jobs cap: free RAM / 8G worker cap, plus per-provider rate-limit stagger
- (idea by coach) headless adapter (omp -p style one-shot) for parallel mode; tmux only for attended single-lane debugging
- (technique by coach) started Pre-mortem (imagine the overhaul failed, work backwards)
- (idea by coach) failure: two lanes both green, merged main broken - mitigation: integration re-verify gate is non-negotiable (already ideated, premortem confirms it as THE gate)
- (idea by coach) failure: 6 parallel MiniMax workers hit provider rate limit, all stall - mitigation: stagger launches + per-lane exponential backoff + jobs cap default LOW (2-3)
- (idea by coach) failure: lock-file / generated-artifact conflicts (pnpm-lock, graphify-out) in every lane - mitigation: generated paths regenerated at merge, never merged
- (idea by coach) failure: blocked ticket starves its whole subtree while driver keeps burning unrelated leaves - OK by design, but driver must report subtree starvation, not silently finish partial
- (idea by coach) failure: driver crash leaves 3 dangling worktrees; naive restart double-claims - mitigation: claim = frontmatter owner+heartbeat commit on the LANE branch; reap stale claims like kanban_reap does
- (idea by coach) failure: disk blowup from N full worktrees w/ node_modules - mitigation: shared pnpm store; skip install for docs tickets; prune lanes on landing
- (idea by coach) failure: user drops new ticket mid-run - driver re-reads board every wave; graph is re-derived per wave, never cached
- (technique by coach) started Analogy: CI merge queues (GitHub merge queue, bors)
- (idea by coach) bors-style: lane must be rebased on latest integration tip and green BEFORE landing; driver auto-rebases lane and re-runs verification - steal this as the landing protocol
- (idea by coach) batch landing: when lanes are docs-only/disjoint, land as one octopus batch, verify once - fallback to bisect the batch on red (bors batching)
- (idea by coach) priority lanes: P0 ticket preempts the merge queue
- (idea by coach) trains: group tiny tickets into one lane to amortize session+worktree overhead (ticket cost != uniform)
- (technique by coach) started Constraint: what if merge conflicts were IMPOSSIBLE
- (idea by coach) decompose-time file-ownership partitioning: to-tickets declares 'files:' scope per ticket; overlapping scopes = automatic dependency edge; scheduler enforces disjoint writes in flight - conflicts become impossible by construction, merge is always ff/trivial
- (idea by coach) worker staying in scope is enforceable: driver diffs lane vs declared scope, out-of-scope diff = bounce (same trick as dsh-board outOfScope)
- (idea by coach) this pushes intelligence to /to-tickets: add a 'parallel-safe' pass that emits scopes + extra edges - upgrade the ticket format, not just the loop
- (technique by coach) started Role Playing (stakeholders: the worker, the reviewer, the merge, future-james-at-8am)
- (idea by coach) worker POV: wants a lane that already has deps installed - lane bootstrap hook (install, env copy) owned by driver, not worker prompt
- (idea by coach) reviewer POV: reviewing per-lane diff is easier than reviewing merged main - keep per-ticket fresh review in lane (unchanged from today), integration gate only re-runs commands
- (idea by coach) future-james POV: wakes up to either everything landed or a one-screen report: landed X, queued Y, bounced Z with reasons - single report artifact .kanban/run-report.md
- (idea by coach) future-james POV: 'undo the whole night' = git revert of the landing merge commits, listed in the report - cheap because landings are ordered
- (idea by coach) shepherd POV: tralph-shepherd needs only sentinel + board reading to babysit N lanes - keep sentinel contract stable so shepherd survives the overhaul
- (technique by coach) started 10x thinking
- (idea by coach) 10x: to-tickets emits the graph AND the schedule (waves precomputed) so any dumb runner can execute it - the plan becomes an artifact you can review before burning tokens
- (idea by coach) 10x: replace polling with git-native events - workers push to lane refs, driver uses post-receive/for-each-ref loop; board state derivable purely from git
- (idea by coach) 10x: run the whole thing as a dsh AgentTeams team with quality-kind tasks (implementation->verification->review chains auto-generated per ticket) but each member cd'd into its own worktree via execution prompt - AgentTeams gives mailboxes, repair rounds, captain takeover for free
- (idea by coach) counter: AgentTeams couples the run to a live dsh session; bash driver survives dsh restarts - hybrid: bash owns lifecycle, optionally spawns AgentTeams inside a lane for big tickets
- (idea by coach) keep tralph CLI name and flags; parallel is just tralph --jobs N (default 1 = today's behavior), overhaul is additive not replacement
- (insight by coach) convergent shape: (1) scopes+edges at ticket time, (2) frontier scheduler, (3) lane worktree per ticket w/ unchanged inner Ralph, (4) bors-style rebase+verify landing queue, (5) crash-only state in git
