---
topic: dsh interactive-learning plugin: sidebar as learner activity surface, chat stays teaching
goal: Shape a buildable dsh plugin moving quizzes/code-practice/recall out of the chat transcript into better-sidebar, honoring teach skill probe->plan->teach and the dsh plugin contract
mode: partner
status: complete
updated: 2026-09-04T02:30
---

- (decision) Stance: Creative Partner (user picked)
- (decision) Technique batch: Assumption Reversal -> Morphological Analysis -> One Feature Only -> Ship in 60 Minutes
- (insight by coach) Sidebar has agent->sidebar push (AgentOpenRegistry/tools) but only appendToDraft as sidebar->agent return leg; the return leg is the thing to invent
- (insight by coach) teach/quiz-ui.md hard-stops if no recognised question tool: plugin must register one the skill's table accepts, or the skill gains a row
- (insight by coach) teach already persists strand maps + mermaid DAG as markdown under .teach/; sidebar renders markdown+mermaid and matches file viewers by ext -> live map view nearly free
- (technique) started Assumption Reversal
- (idea by coach) A2 reversed: sidebar is not where the learner ANSWERS but where the learner WORKS continuously; chat interrupts the workbench. Teacher watches live artifact state (false starts included) instead of receiving submissions
- (direction by user) A2 flip accepted; wants help generating the A1 flip
- (idea by coach) A1-flip-1: the answer STAYS in chat; what leaves is the logistics. Sidebar = teacher's instrument panel (mermaid DAG, strand map, citations, code examples, review queue) - the things teach already writes to .teach/
- (idea by coach) A1-flip-2: what leaves is the GRADING - learner answers in prose in chat, sidebar shows the ledger moving (edge->known, confidence, FSRS card scheduled)
- (idea by coach) A1-flip-3: what leaves is the QUESTION - chat only teaches, sidebar holds pending questions answered whenever; decouples teaching from testing in TIME (what a transcript fundamentally cannot do)
- (decision by user) Flip-1 ACCEPTED: the sidebar is an INSTRUMENT PANEL. Core metaphor locked.
- (question by user) If the teacher asks for input on the panel, is it captured and scored? Learner needs feedback, teacher needs to see it
- (insight by coach) VERIFIED in sidechat-routes.ts:117-146: agent.followup(createUserMessage) admits a user message via the stock path (wakes driver, logs user/message); agent.inject(createUserMessage{source:{kind:plugin}}) queues model-facing context that does NOT wake the agent and renders as a collapsed context row not a chat bubble. ctx.get('agents').get(id) resolves the live agent. Sidechat aims these at a CHILD by choice, not constraint.
- (decision by user) Option 2 (real return leg) chosen over draft-fill and file-drop: learner needs feedback, teacher needs visibility
- (idea by coach) THE KEY MAPPING: followup = learner wants a verdict now (wakes teacher, scores in chat); inject = workings/false starts ride along silently, teacher sees them next turn with NO chat bubble. Solves 'visible to teacher, invisible in transcript'
- (question by coach) PREFLIGHT before build: ask dsh-plugin-guide whether agent.followup/agent.inject are sanctioned third-party APIs or sidebar-internal (read off installed 0.17.1 source, not documented contract)
- (idea by user) Panel content: generated images, mermaid charts; flowcharts to show process alongside the main chat
- (decision by user) SCOPE for now: only quiz + code snippets as INTERACTIVE activities. Mermaid/flowcharts/images are DISPLAY (teacher output), not learner input
- (insight by coach) RETRACTED flight-recorder-as-keystroke-stream: per-keystroke inject floods context+storage, leaks unconsented drafts. Correct form = debounced, aggregated, consented structured summary (e.g. one attempt-summary per card: attempts, time-to-answer, ran-and-failed count)
- (decision by user) BOTH quiz answer and code snippet submit use agent.followup (wake the teacher, get a verdict). No silent-iteration mode.
- (idea by coach) Mermaid DAG pinned in panel = live progress bar (current node highlighted, locked nodes solid, inserted prereq appears on quiz failure) - transcript can only show where you've BEEN
- (idea by coach) Flowchart-as-quiz: render DAG with a node blanked, learner fills it. Tests structure not recall; suits process-shaped material
- (insight by coach) Both-followup means inject is NOT needed for MVP. Single return path = simpler build; inject stays available for the debounced attempt-summary rider on the same followup
- (technique) started Morphological Analysis
- (decision by user) P4 = GREEN LIGHT: verdict lands in the panel (card turns green/red); chat carries only the WHY. A correct answer costs the thread nothing - no 'Correct!' turn.
- (decision by coach) Morphological picks endorsed: P1 teacher-authored live, P2 defineTool (file-watch rejected: no card identity/session scope/delivery confirmation), P3 mortal cards (FSRS resurfacing = later, needs durable storage+scheduler), P5 DAG at rest
- (insight by coach) Green light has a consequence: on a CORRECT answer the teacher may not need to speak at all -> followup must tolerate a no-op turn, or the verdict returns as the tool RESULT rather than a new turn. Design question for the build.
- (technique) started One Feature Only
- (decision by user) One-Feature answer: B or C, NOT A alone. If B (live DAG) it must ALSO deliver and switch to quizzes and code snippets - i.e. the DAG is the shell/navigator, not a standalone display
- (decision by user) CLARIFIED: 'code snippet' = a mini CODING CHALLENGE for the learner (write code to satisfy a task), not a read-only example to view
- (insight by coach) User's B-with-delivery answer reframes the architecture: the ONE feature is not a card type at all, it's the PANEL AS LESSON SHELL - a persistent surface showing where you are (DAG) that the teacher swaps activity views into. Card types become plugins of the shell.
- (direction by user) Reframe: this is NOT a new idea - only the dsh implementation is new. Standard pattern = learning content LEFT, interactive part RIGHT. Asked for prior-art web search.
- (decision by coach) EXECUTION SAFETY (blocker advisory accepted): sidebar's session-scoped terminal is NOT a safe runner for learner code (real machine, real secrets). Explicit Run only, no auto-run; constrained sandbox with time/memory/network/fs limits is a precondition of shipping any code execution.
- (insight by coach) Prior art: Codecademy 3-pane (narrative left / editor centre / terminal-output right); futurecoder splits editor+shell vertically with instructions beside; Kaggle Learn users REQUESTED side-by-side split for exercises; the left-content/right-interaction split is the industry default, so dsh chat-left + sidebar-right already IS the canonical layout
- (direction by user) Over-engineering flagged: emulate the common pattern inside dsh, don't reinvent. Simplest implementation wins.
- (insight by coach) CORRECTION: 'industry default' overstated (a Kaggle feature request + DeepWiki pages are weak evidence); honest claim = common pattern in products reviewed. The three 'nobody has this' differentiators were unverified negatives.
- (idea by coach) Ship-in-60 MVP: one tab (registerTab id 'learn'), one tool (teacher pushes question+options+answerKey), one return path (agent.followup on submit), green light graded locally from the answer key, DAG at rest = open the mermaid file teach already writes. Deleted: code execution, FSRS, images, flowchart-quiz, attempt-summary rider, inject.
- (decision by user) CONVERGE (option 1): write the shaped one-pager with build order and preflight questions
- (insight by coach) PREFLIGHT 1 CLOSED: mermaid renders free - MermaidMarkdown swaps language-mermaid fences for diagrams in the md preview (lazy chunk). DAG at rest = open the file teach already writes, zero code.
- (insight by coach) PREFLIGHT 2 CLOSED: agent.followup/inject are OFFICIAL public API - docs/subsystems/core.md:55 'Agent is the surface every plugin (UI, hooks, orchestrators) programs against; ctx.agents.get(id) returns it; followup/steer/inject are fixed-preset aliases of send'. DSH's own skills subsystem + scheduler use them. CAVEAT: followup() returns no handle, MessageId = inbox insertion not a later answer.
- (insight by coach) PREFLIGHT 3 CLOSED: plugin-added SessionEventMap events are LOG-ONLY (session.md:595), not surface events - so a custom event is WRONG here. followup already makes a proper user/message surface event. No new event type needed. adding-a-tool.md documents exec.agent + agent.inject as the sanctioned async-notification path.
- (decision by coach) Convergence move: MoSCoW (scoping a build)
- (decision by coach) MoSCoW settled. MUST=one tab + one defineTool card push + followup return + local green light + mermaid at rest. SHOULD=badge, debounced attempt-summary via inject, keyboard reach. COULD=coding challenge (gated on sandbox), FSRS, strand map, flowchart-quiz. WONT=any code execution without a constrained sandbox, per-keystroke telemetry, images, card banks, multiple tabs.
- (decision by coach) Build order: preflight(done) -> dsh-plugin-dev new -> implement (host tool+queue+followup / client tab+card+local grading) -> plugin_check strict + check --strict + verify -> dsh plugin --profile web add -> ADD A ROW TO quiz-ui.md or teach will not use the panel
- (question by coach) OPEN: who grades (local answerKey vs teacher-pushed verdict) matters for partially-right answers; quiz-ui.md edit vs tool-name alias; empty-state beyond the DAG
- (note) Artifact written: SHAPED.md
- (decision by user) GRADING RESOLVED: the teacher (main LLM session) grades, NOT a local answer key. Panel sends the answer via followup; teacher returns the verdict; panel renders green/red from what the teacher pushes back.
- (note) Handoff written to /tmp/handoff-96PWqj.md; SHAPED.md updated for teacher-grades decision (learn_card loses answerKey, pending state + cardId round trip added to open questions)
