{{TASK_GIVEN_TO_THE_SMALL_MODEL}}

This is the `user` message. The SKILL.md being tuned is sent separately as the
`system` message by verify.sh. Write a concrete input that forces the small
model to exercise ONE critical behavior of the skill. Keep it realistic and
minimal — do not hand-hint the answer.
