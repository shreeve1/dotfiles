const test=require('node:test'), assert=require('node:assert/strict'), M=require('../Model.js');
const C='/tmp/touchpadctl';
test('validates and builds exact CLI ordering',()=>{const s={sensitivity:0,scroll_factor:.4,natural_scroll:false,tap_to_click:true,clickfinger_behavior:true,disable_while_typing:true,tap_and_drag:true,middle_button_emulation:false};assert.deepEqual(M.statusCommand(C),[C,'status','--json']);assert.deepEqual(M.restoreCommand(C),[C,'restore','--json']);assert.deepEqual(M.applyCommand(C,s).command,[C,'apply','--json',JSON.stringify(s)]);});
test('validates ranges and booleans',()=>{let s={sensitivity:1,scroll_factor:2,natural_scroll:true,tap_to_click:true,clickfinger_behavior:true,disable_while_typing:true,tap_and_drag:true,middle_button_emulation:true};assert.equal(M.validSettings(s),true);assert.equal(M.validSettings({...s,sensitivity:1.01}),false);assert.equal(M.validSettings({...s,scroll_factor:.15}),false);assert.equal(M.validSettings({...s,tap_to_click:1}),false);});
test('parses stable response',()=>{const s={sensitivity:0,scroll_factor:.4,natural_scroll:false,tap_to_click:true,clickfinger_behavior:true,disable_while_typing:true,tap_and_drag:true,middle_button_emulation:false};assert.equal(M.parseResponse(JSON.stringify({schemaVersion:1,ok:true,settings:s})).ok,true);assert.equal(M.parseResponse('nope').ok,false);});
test('extracts human-readable structured failures',()=>{const p=M.parseResponse(JSON.stringify({schemaVersion:1,ok:false,error:'hyprctl unavailable'}));assert.deepEqual(p,{ok:false,error:'hyprctl unavailable'});});
