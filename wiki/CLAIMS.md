# Claims Registry

| ID | Claim | Source | Page | Confidence | Status | Notes |
| C-0001 | Example claim. | `wiki/raw/example.md` | `wiki/concepts/example.md` | medium | example | Replace or supersede when real claims are added. |
| C-0106 | `rralph` runs the RPIV skill chain unattended, one step per fresh interactive engine TUI on the default tmux server, handing off via on-disk artifacts in `.rpiv/artifacts/<type>/` rather than shared context. | `bin/rralph` | `wiki/analyses/rpiv-pipeline.md` | high | active | Session-derived; cites driver header lines 1-22. |
| C-0107 | The pipeline order is research → design → plan → implement → validate → code-review → commit, with discover run by hand first and the driver picking up the newest FRD. | `bin/rralph` | `wiki/analyses/rpiv-pipeline.md` | high | active | Session-derived; `bin/rralph:52`, `:112`. |
| C-0108 | `rralph` defaults to the `pi` engine; `--engine claude` or `RPIV_ENGINE` opts into Claude as the coding agent. | `bin/rralph` | `wiki/analyses/rpiv-pipeline.md` | high | active | Session-derived; `bin/rralph:31`, `:55`. |
| C-0109 | Each `rralph` run works on a fresh branch `${RPIV_BRANCH_PREFIX:-rpiv}/<TS>` created with `git checkout -b` — a branch in the repo's own working dir, not a git worktree. | `bin/rralph` | `wiki/analyses/rpiv-pipeline.md` | high | active | Session-derived; `bin/rralph:116`, `:175`. |
| C-0110 | A `gap-sweep` runs after FRD/research/design/plan and after implement: a fresh AI session fixes critical gaps in the just-produced artifact in place and continues, auto-accept with no gate. | `bin/rralph` | `wiki/analyses/rpiv-pipeline.md` | high | active | Session-derived; `bin/rralph:292-307`, `.claude/skills/gap-sweep/SKILL.md`. |
| C-0111 | `rpiv-monitor` diagnoses a running pipeline strictly read-only via `tmux capture-pane`; attaching or send-keys to the live pane corrupts the driver's paste/marker detection. | `.claude/skills/rpiv-monitor/SKILL.md` | `wiki/analyses/rpiv-pipeline.md` | high | active | Session-derived. |
| C-0112 | The live rralph socket must be found from the driver's tmux session list, not `ls -t`, because a stale socket file can sort first by mtime while having no live server. | `.claude/skills/rpiv-monitor/SKILL.md` | `wiki/analyses/rpiv-pipeline.md` | high | active | Session-derived. |
| C-0113 | `rpiv-merge` lands a finished pipeline branch: enumerate `rpiv/<TS>` branches, review the diff, test, then merge — confirming before merge, never pushing unless asked, with a dirty-tree guard before checkout. | `.claude/skills/rpiv-merge/SKILL.md` | `wiki/analyses/rpiv-pipeline.md` | high | active | Session-derived. |
| C-0114 | The driver persists the kickoff fork point to `.rpiv/run/<TS>/.base` so `rpiv-merge` targets the exact base; older runs without `.base` fall back to `origin/HEAD` then `init.defaultBranch`. | `bin/rralph` | `wiki/analyses/rpiv-pipeline.md` | high | active | Session-derived; only helps runs created after the change. |
| C-0115 | A discover session run in interactive Claude can feed a pi-driven `rralph` pipeline, because handoff is file-based (the FRD); the producing engine is irrelevant given the same repo/cwd and pi onboarded there. | `bin/rralph` | `wiki/analyses/rpiv-pipeline.md` | medium | active | Session-derived reasoning over `bin/rralph:112`; not yet exercised end-to-end. |

Claim IDs use the next available zero-padded integer in `C-0001` format. Before adding claims, scan existing `C-####` IDs in this file, find the maximum, and increment by one for each new claim.
