# Claims Registry

| ID | Kind | Claim | Source | Page | Confidence | Status | Created | Hits | Superseded | Impact | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| C-0001 |  | Example claim. | `wiki/raw/example.md` | `wiki/concepts/example.md` | medium | example |  | 0 |  |  | Replace or supersede when real claims are added. |
| C-0106 | config-fact | `rralph` runs the RPIV skill chain unattended in the order research → design → plan → implement → validate → code-review → commit, one step per fresh interactive engine TUI on the default tmux server, handing off through `.rpiv/artifacts/<type>/`; discover is run by hand first and the driver picks up the newest FRD. | `bin/rralph` | `wiki/analyses/rpiv-pipeline.md` | high | active | 2026-06-04 | 0 |  | Knowing the handoff is file-based, not shared context, prevents debugging a "lost context" non-bug between steps. | Session-derived; cites driver header lines 1-22. |
| C-0108 | config-fact | `rralph` defaults to the `pi` engine; `--engine claude` or `RPIV_ENGINE` opts into Claude as the coding agent. | `bin/rralph` | `wiki/analyses/rpiv-pipeline.md` | high | active | 2026-06-04 | 0 |  | Wrong default engine wastes a full unattended pipeline run before the mistake is noticed. | Session-derived; `bin/rralph:31`, `:55`. |
| C-0109 | config-fact | Each `rralph` run works on a fresh branch `${RPIV_BRANCH_PREFIX:-rpiv}/<TS>` created with `git checkout -b` — a branch in the repo's own working dir, not a git worktree. | `bin/rralph` | `wiki/analyses/rpiv-pipeline.md` | high | active | 2026-06-04 | 0 |  | Expecting a worktree leads to looking in the wrong path and mishandling the dirty tree at kickoff. | Session-derived; `bin/rralph:116`, `:175`. |
| C-0110 | config-fact | A `gap-sweep` runs after FRD/research/design/plan and after implement: a fresh AI session fixes critical gaps in the just-produced artifact in place and continues, auto-accept with no gate. | `bin/rralph` | `wiki/analyses/rpiv-pipeline.md` | high | active | 2026-06-04 | 0 |  | Knowing gap-sweep auto-edits artifacts in place explains unexpected mid-run artifact changes instead of treating them as corruption. | Session-derived; `bin/rralph:292-307`, `.claude/skills/gap-sweep/SKILL.md`. |
| C-0111 | gotcha | `rpiv-monitor` diagnoses a running pipeline strictly read-only via `tmux capture-pane`; attaching or send-keys to the live pane corrupts the driver's paste/marker detection. | `.claude/skills/rpiv-monitor/SKILL.md` | `wiki/analyses/rpiv-pipeline.md` | high | active | 2026-06-04 | 0 |  | Attaching to the live pane to "check on it" silently derails the running pipeline; read-only only. | Session-derived. |
| C-0112 | gotcha | The live rralph socket must be found from the driver's tmux session list, not `ls -t`, because a stale socket file can sort first by mtime while having no live server. | `.claude/skills/rpiv-monitor/SKILL.md` | `wiki/analyses/rpiv-pipeline.md` | high | active | 2026-06-04 | 0 |  | Using `ls -t` to find the socket attaches to a dead server and wastes a debugging cycle on a phantom. | Session-derived. |
| C-0113 | runbook-step | `rpiv-merge` lands a finished pipeline branch: enumerate `rpiv/<TS>` branches, review the diff, test, then merge — confirming before merge, never pushing unless asked, with a dirty-tree guard before checkout. | `.claude/skills/rpiv-merge/SKILL.md` | `wiki/analyses/rpiv-pipeline.md` | high | active | 2026-06-04 | 0 |  | Following the guarded order avoids checking out over a dirty tree or pushing an unreviewed unattended branch. | Session-derived. |
| C-0114 | config-fact | The driver persists the kickoff fork point to `.rpiv/run/<TS>/.base` so `rpiv-merge` targets the exact base; older runs without `.base` fall back to `origin/HEAD` then `init.defaultBranch`. | `bin/rralph` | `wiki/analyses/rpiv-pipeline.md` | high | active | 2026-06-04 | 0 |  | Knowing the `.base` fallback chain prevents merging an old run against the wrong base and pulling in unrelated diff. | Session-derived; only helps runs created after the change. |
| C-0115 | config-fact | A discover session run in interactive Claude can feed a pi-driven `rralph` pipeline, because handoff is file-based (the FRD); the producing engine is irrelevant given the same repo/cwd and pi onboarded there. | `bin/rralph` | `wiki/analyses/rpiv-pipeline.md` | medium | active | 2026-06-04 | 0 |  | Confirms a mixed-engine discover→run is safe, avoiding a needless re-run of discover under pi. | Session-derived reasoning over `bin/rralph:112`; not yet exercised end-to-end. |
| C-0116 | decision | ponytail (YAGNI enforcer) is vendored into both the Claude Code and Pi harnesses, never installed via `/plugin marketplace add` or `pi install git:`, because both install paths write machine-local state that does not sync across the dotfiles machines. | CLAUDE.md |  | high | active | 2026-06-23 | 0 |  | Stops an agent from repairing ponytail by plugin/pi-installing it, which silently breaks the cross-machine sync model the dotfiles repo depends on. |  |
| C-0117 | gotcha | ponytail SessionStart+UserPromptSubmit hook entries must exist in both `.claude/settings.json.template` and every machine-local `.claude/settings-*.json` provider file, because switch-provider.sh copies one provider file over live `~/.claude/settings.json`. | CLAUDE.md |  | high | active | 2026-06-23 | 0 |  | Explains ponytail going silently dark after a provider switch as a missing-hook-entry config gap, not a ponytail bug, saving a wrong-direction debugging cycle. |  |
| C-0118 | config-fact | Wiki claims enter wiki/CLAIMS.md only through gate.py with an ADMIT verdict (IDs/timestamps assigned by the gate); hand-editing rows fails `gate.py audit`, and gated consolidation refuses to run without a non-empty wiki/eval/*.eval slice. | .claude/skills/wiki-update/Workflows/SessionUpdate.md |  | high | active | 2026-06-23 | 0 |  | Prevents a future session hand-editing CLAIMS.md (which fails the verification audit) or running an unverifiable consolidation that silently drops load-bearing claims. |  |
| C-0119 | decision | Verification is two layers: pi-duo is the cheap constant in-band grounding gate (catches false/unsupported claims); a separate on-demand completeness review (fresh tooled reviewer, omission-focused prompt) catches material omissions at task boundaries. The two are different mechanisms at different cost profiles and must not be collapsed. | docs/adr/0001-verification-two-layers.md | CONTEXT.md | high | active | 2026-07-21 | 0 |  | Stops expecting pi-duo to catch things-the-work-missed (it cannot, by prompt design) and stops building a 'stronger grounding gate' to fix a completeness problem; reaches for the completeness-review layer instead. |  |
| C-0120 | config-fact | pi-duo's verifier runs via completeSimple() (a single text completion with NO tools) and verifierContext returns {systemPrompt, messages} only — it cannot read repo files; it checks the answer against the flattened text transcript. So it can only catch a false claim whose contradicting evidence is already in the transcript. | wiki/raw/sessions/2026-07-21-gap-review-completeness-layer.md | docs/adr/0001-verification-two-layers.md | high | active | 2026-07-21 | 0 |  | Explains why pi-duo passes confidently-wrong claims that aren't contradicted in-transcript, and stops trying to tune pi-duo to ground claims against the repo — it is structurally a transcript-consistency check, not a repo-grounding check. |  |
| C-0121 | gotcha | pi's assistant content-block type for a tool call is 'toolCall' (camelCase), not Anthropic's 'tool_use'; the tool name is block.name and arguments live in block.arguments (the read tool reports .path, not .file_path). Parsing tool_use/input silently no-ops. | wiki/raw/sessions/2026-07-21-gap-review-completeness-layer.md |  | high | active | 2026-07-21 | 0 |  | Stops an agent writing tool_use/input-based message or tool-call parsing in a Pi extension that silently never matches, producing a no-op extension that looks installed-but-broken. |  |
| C-0122 | gotcha | pi fires turn_end and before_agent_start per agent-STEP, not per user-turn; a tool step (turnIndex N) and the terminal answer (turnIndex N+1) are separate events. Clearing a per-turn accumulator at turn_start/before_agent_start wipes values accumulated during the tool step before the terminal turn_end consumes them — clear only at the terminal turn_end. | wiki/raw/sessions/2026-07-21-gap-review-completeness-layer.md |  | high | active | 2026-07-21 | 0 |  | Stops the recurring 'accumulator cleared too early' bug class in turn-scoped Pi-extension state (files captured mid-turn vanish before the final-answer hook reads them). |  |
| C-0123 | gotcha | dsh-board stage handlers/allowlisted-actions are agent-followed prose, not code: each tick is a kind:agent cron job whose rendered prompt points at HANDLERS.md and the tick agent runs the git commands itself; there is no executor interpreting action names. So a stage-behavior fix is an edit to HANDLERS.md/preamble.md, never a code change. | dsh-board/preamble.md | wiki/analyses/dsh-board-pipeline.md | high | active | 2026-09-04 | 0 |  | Stops a future session hunting for a nonexistent tick executor to implement a new action, or writing code when the fix is a prose contract edit. |  |
| C-0124 | gotcha | dsh-board HANDLERS.md is read live via the ~/.dsh-boards/dotfiles/HANDLERS.md symlink (edits apply on the next tick, no restart), but preamble.md is rendered into FROZEN copies inside the six cron prompts by render-jobs.sh --install and only takes effect after a dsh restart. A preamble edit without regenerate+restart silently does not deploy. | dsh-board/INSTALL.md | wiki/analyses/dsh-board-pipeline.md | high | active | 2026-09-04 | 0 |  | Prevents assuming a preamble/allowlist change is live (it is not until render-jobs.sh --install + restart) while HANDLERS changes already are, avoiding a half-deployed board. |  |
| C-0125 | gotcha | An agent_teams team is permanently bound to its creating captain session (findTeamByCaptain matches team.captainSessionId set once at creation); there is no adopt/re-attach API, and the scheduler advances a member to its next ready task only while that captain's process is alive. A short-lived Build/Review tick that dispatches a dependent-task chain then exits strands every task after the first. | /home/james/.dsh/profiles/web/node_modules/@nanmicoder/dsh-agent-teams/lib/scheduler.js | wiki/analyses/dsh-board-pipeline.md | high | active | 2026-09-04 | 0 |  | Explains the Build<->Decompose infinite loop (task1 done, captain dies, task2+ strand, bounce wipes lane, repeat) and rules out a re-attach fix that the API cannot support. |  |
| C-0126 | decision | dsh-board Build must create ONE composite task listing all breakdown steps in order for one engineer to drive in a single turn, never a chain of dependent tasks; a genuine stall then means the spec is too big for one Build turn and Decompose splits it into smaller CARDS, not dependent tasks. | dsh-board/HANDLERS.md | wiki/analyses/dsh-board-pipeline.md | high | active | 2026-09-04 | 0 |  | Encodes the fix for the captain-death stall loop so a future edit does not reintroduce dependent Build tasks and re-break the pipeline. |  |
| C-0127 | decision | dsh-board Merge stage auto-runs git merge --ff-only itself (no human gate), strictly fast-forward only: a diverged lane bounces to Build, git's own --ff-only and dirty-tree refusals mean the board can never make a merge commit, force, or overwrite uncommitted work, and a post-merge check.sh failure blocks for a human. git-merge-ff is the single WRITE action in the preamble allowlist. | dsh-board/HANDLERS.md | wiki/analyses/dsh-board-pipeline.md | high | active | 2026-09-04 | 0 |  | Records that the board is now fully autonomous through Merge and the exact safety rails, so a future change does not loosen --ff-only into a merge that could clobber main. |  |
| C-0128 | gotcha | cron_disable writes a persisted conversational OVERRIDE (enabledSource: override) that survives a dsh restart and does not change the config file's enabled flag; a disabled tick stays disabled across restart and must be re-enabled with cron_enable. | wiki/raw/sessions/2026-09-04-dsh-board-loop-fixes.md | wiki/analyses/dsh-board-pipeline.md | high | active | 2026-09-04 | 0 |  | Stops assuming a restart re-enables a cron_disable'd tick (it does not), avoiding a silently-dark board stage after a deploy restart. |  |
| C-0129 | gotcha | All six dsh-board ticks originally shared */15 * * * *, firing together and bursting the LLM provider into a MiniMax 429 Token-Plan rate limit even with quota left; render-jobs.sh staggers each stage 2 min apart (spec :00 .. merge :10) which clears the 429. | dsh-board/render-jobs.sh | wiki/analyses/dsh-board-pipeline.md | high | active | 2026-09-04 | 0 |  | Points a future 429-on-ticks straight at simultaneous firing (stagger), not a quota purchase or provider switch. |  |
| C-0130 | decision | Portable user-owned Omarchy configuration is stored under omarchy/ and linked by install.sh only on Linux when the omarchy command exists; packaged runtime content under ~/.local/share/omarchy is intentionally reinstalled rather than synced. | install.sh; omarchy/README.md | wiki/candidates/analysis-session-omarchy-bare-metal-and-surface-recovery.md | high | active | 2026-09-19 | 0 |  | Prevents copying package-owned or cross-architecture Omarchy runtime trees into another machine and breaking the desktop with stale binaries or overwritten package state. | Grounded in install.sh:210-220 and omarchy/README.md:1-24; independent checker unavailable because the configured provider had no API key, so fallback line-level re-grounding was used. |
| C-0131 | config-fact | The managed Hyprland setup runs hypr-deck as a user service for focus-driven overlapping windows, and persistent workspace rules assign workspace 1 to laptop output eDP-1 and workspaces 2 through 10 to external output DP-4. | omarchy/README.md; omarchy/config/hypr/monitors.lua; omarchy/config/systemd/user/hypr-deck.service | wiki/candidates/analysis-session-omarchy-bare-metal-and-surface-recovery.md | high | active | 2026-09-19 | 0 |  | Explains both the required Deck daemon and the host's fixed workspace ownership, avoiding diagnosis of intentional display placement as random Hyprland workspace swapping. | Grounded in omarchy/README.md:5-16,60-101 and monitors.lua:10-20; live Hyprland rules were also verified during the session. |
| C-0132 | config-fact | Surface Laptop 7 Intel recovery has four host-specific areas: the SAM node-group module restores keyboard/battery/AC, userspace iptsd restores touchpad behavior, a patched SoundWire module plus UCM overlay restores audio, and a one-shot service conditionally reloads btintel_pcie when no Bluetooth controller registers. | omarchy/README.md; omarchy/HARDWARE-surface-laptop-7.md | wiki/candidates/analysis-session-omarchy-bare-metal-and-surface-recovery.md | high | active | 2026-09-19 | 0 |  | Provides the correct recovery map when hardware disappears, preventing unrelated fixes from being applied to the wrong subsystem and preserving reproducibility after reinstall. | Grounded in omarchy/README.md:107-132 and the detailed hardware runbook. |
| C-0133 | runbook-step | After a linux-omarchy kernel update, reboot into the new kernel before rebuilding the version-bound keyboard/battery and audio modules, reboot again after restoration, then run the full Surface verifier; the userspace touchpad repair does not require a kernel-update rebuild. | omarchy/HARDWARE-surface-laptop-7.md | wiki/candidates/analysis-session-omarchy-bare-metal-and-surface-recovery.md | high | active | 2026-09-19 | 0 |  | Prevents rebuilding modules for the old uname -r, which would leave the new kernel without keyboard, battery, or audio fixes and can make the encrypted-root boot difficult to recover. | Grounded in omarchy/HARDWARE-surface-laptop-7.md:161-172,238-241,397-438. |

Claim IDs use the next available zero-padded integer in `C-0001` format. Schema and bounding gates enforced by `gate.py` (see `.claude/skills/wiki-update/`).
