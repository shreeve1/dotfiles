# Team Knowledge Base

- Verify repo layout, tooling, and architectural boundaries from the checked-in code and config instead of assuming conventions.
- Prefer established local patterns over generic defaults when planning or implementing work.
- Team wiring lives under `agents/teams/<team>/`; agent prompts are frontmatter + markdown, session notes are JSONL, and expertise files are markdown.
