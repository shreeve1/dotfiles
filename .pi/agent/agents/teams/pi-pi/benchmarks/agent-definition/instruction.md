# Scenario: Create a Database Expert Agent

You are the agent-expert for the pi-pi framework team. A user asks:

---

"I want to add a 'database-expert' agent to my development team. It should specialize in PostgreSQL — query optimization, index design, schema migrations, and performance diagnostics. It should be able to read the codebase to understand the data model, run EXPLAIN ANALYZE on queries, and suggest improvements. It should NOT be able to modify production data directly.

What should the agent definition look like?"

---

Produce the complete agent definition .md file, following pi's agent format and conventions.
