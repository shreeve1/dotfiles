# Benchmarks — Pi-Pi Framework Experts Team

Each benchmark tests whether the framework experts give accurate, actionable, and format-compliant advice about the pi agent framework.

## Format

Same as the full team — see `~/.pi/agent/agents/teams/1-full/benchmarks/README.md` for details.

## Domain Notes

Benchmarks test pi framework knowledge: agent definitions, skills, extensions, themes, TUI, configuration. Accuracy against the actual pi API and conventions is critical.
