# Team Knowledge Base

- Verify repo layout, tooling, and boundaries from code/config — don't assume conventions.
- Prefer established local patterns over defaults.
- Team files live under `agents/teams/<team>/`: agent prompts (frontmatter + markdown), session notes (JSONL), expertise (markdown).
