# Claude Code Team

Full pipeline team tuned for reliable dispatching from Claude Code's /pi skill. Uses minimax/MiniMax-M2.7 instead of zai/glm-5.1 to avoid silent request drops. Same agents and capabilities as the `full` team.
